<%= form_for(swot_table) do |f| %>
  <% if swot_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(swot_table.errors.count, "error") %> prohibited this swot_table from being saved:</h2>

      <ul>
      <% swot_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br>

    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <div class="field">
            <%= f.label :title %>
            <%= f.text_field :title, class: 'form-control' %>
          </div>
        </div>
      </div>

      <br>

      <div class="row">
        <div class="col-lg-1">
        </div>
        <div class="col-lg-5 gauche">
          <div class="field">
            <%= f.label :strength %> <br>
            <%= f.text_area :strength, class: 'form-control' %>
          </div>

          <div class="field">
            <%= f.label :opportunity %> <br>
            <%= f.text_area :opportunity, class: 'form-control' %>
          </div>
        </div>

        <div class="col-lg-5 corps">
          
          <div class="field">
            <%= f.label :weakness %> <br>
            <%= f.text_area :weakness, class: 'form-control' %>
          </div>

          <div class="field">
            <%= f.label :threats %> <br>
            <%= f.text_area :threats, class: 'form-control' %>
          </div>
        </div>
        <div class="col-lg-1">
        </div>
      </div>
    </div>

  <br>
  <div class="actions">
    <%= f.submit 'SWOT', class: 'btn btn-outline-success' %>
  </div>
<% end %>
<style>

  textarea{
    height:200px;
    width:100%;
  }
</style>